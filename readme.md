### Hello, Git! 

git init  : to initialize the git into our currect directory

git add or git add ./ : to add file 

git commit -m : to commit changes to main branch

git log : to check what changes have been done

git checkout : to check out branches

git checkout 'main': to checkout main branch

git checkout -f 'main': to checkout main branch by force


git status : to check the status of the git branches

git branch -m main: 

git remote add origin "linkofgithubrepo"

git push -u origin main : to push repo to mian branch

git checkout -b feature-branch : creating new branch names feature-branch

git branch new-branch-name source-branch:

I'm adding this from 'geature-branch'